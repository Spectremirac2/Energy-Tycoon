[build]
  command = "npm run build:client"
  publish = "dist/public"

# SPA fallback — tüm route'ları index.html'e yönlendir
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
